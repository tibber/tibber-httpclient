machine:
  node:
    version: 6.2.0
deployment:
  npm:
    branch: master
    commands:
      - npm run build
      - echo -e "$NPM_USERNAME\n$NPM_PASSWORD\n$NPM_EMAIL" | npm login
      - npm run 2npm
